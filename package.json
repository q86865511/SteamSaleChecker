{
  "name": "steamsalechecker",
  "private": true,
  "type": "module",
  "workspaces": ["shared", "worker", "web", "api"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p shared && tsc --noEmit -p api && tsc --noEmit -p worker && tsc --noEmit -p web"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^4.0.0",
    "tsx": "^4.16.0",
    "@types/node": "^20.14.0"
  },
  "overrides": {
    "undici": "^6.27.0"
  }
}
